export type Paths = '/' | '/community' | '/footprint' | '/result';
export type Colors = 'primary' | 'secondary' | 'accent' | 'info';

export interface ColorConfigProps {
  bg: string;
  text: string;
}
